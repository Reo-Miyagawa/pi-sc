<div class="index">
  <div class="toppage">
    <div class="listwrapper">
      <% @lists.each do |list| %>
      <div class="list">
        <div class="list_header">
          <div class="list_header_title"><%= list.title %></div>
        <div slass="list_header_action">
          <%= link_to "a", edit_list_path(list.id), class: "edpen" %>
        </div>
      </div>
      <% end %>
    </div>
  </div>
</div>